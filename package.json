{
  "name": "dofus-hunt-overlay",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "electron-transparency-mouse-fix": "^1.0.0-rc.1",
    "typescript": "~3.7.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:electron": "cross-env TS_NODE_PROJECT=./tsconfig.electron.json NODE_ENV=development electron -r ts-node/register src/main/index.ts",
    "build:electron": "tsc -p tsconfig.electron.json",
    "package": "gulp"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "@types/electron": "^1.6.10",
    "electron": "^8.5.2",
    "electron-packager": "^14.2.1",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.2.0",
    "cross-env": "^7.0.2",
    "del": "^5.1.0",
    "ts-node": "^8.9.0"
  }
}
